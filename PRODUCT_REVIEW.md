# 🚗 Automotive Design Studio Workspace — 产品功能全览

> **产品定位**: 高级汽车设计专家工作台 · 五步法量化评价 · 设计空间分析  
> **技术栈**: Python 3.13 + Streamlit + Pandas + Plotly + SciPy  
> **版本**: v3.0 | **数据**: 136 款真实车型  
> **最后更新**: 2026-07-23

---

## 一、产品架构总览

```
┌────────────────────────────────────────────────────────┐
│                  🚗 Vehicle Visual Studio              │
│  [正侧] [正前] [正后] [前45°] [后45°]      [🎯雷达图] │
│  5视角实车图片 / 占位图                   5步法概览    │
├────────────────────────────────────────────────────────┤
│                🔬 Design Space Analysis                │
│  ┌─📐比例─┬─🚙姿态─┬─✨型面─┬─💎细节─┬─⚙️工程─┐   │
│  │ X/Y轴  │ X/Y轴  │ X/Y轴  │ X/Y轴  │ X/Y轴  │   │
│  │ 下拉框 │ 下拉框 │ 下拉框 │ 下拉框 │ 下拉框 │   │
│  │ 散点图 │ 散点图 │ 散点图 │ 散点图 │ 散点图 │   │
│  │ 灰色竞品+密度等高线+红色★在研+绿色◆爆款  │   │
│  └────────┴────────┴────────┴────────┴────────┘   │
├────────────────────────────────────────────────────────┤
│ 🎯 侧边栏                                              │
│ 项目代号 | 风格选择 | 📷图片上传(5视角)               │
│ 🔍 车格过滤 | 风格过滤 | 竞品搜索多选                 │
│ 📐 10参数滑块(动态默认值=竞品均值)                    │
│ 🔄 刷新数据库                                         │
└────────────────────────────────────────────────────────┘
```

---

## 二、数据管理能力

### 2.1 真实数据库驱动
| 属性 | 详情 |
|------|------|
| 数据源 | `benchmark data.xlsx` 本地 Excel |
| 数据量 | **136 款车型**，29 列原始数据 |
| 覆盖车型 | 小米SU7、汉EV、Model 3、理想L7 等国产及合资主流车型 |
| 车格覆盖 | 大型 / 中型 / 小型 / 紧凑 / 紧凑型 |
| 风格覆盖 | 运动 / 商务 / 硬派 / 稳重 |
| 类型覆盖 | 轿车 / SUV / MPV / 皮卡 |

### 2.2 智能数据清洗
- **百分比自动清洗**: `0.461 → 46.1%`（小数比例）、`"42.5%" → 42.5`（含%字符串）
- **缺失值处理**: 数值列用均值自动填充
- **布尔列解析**: 自动识别 "是"/"✓"/"true" 等
- **列名标准化**: 去除 Excel 换行符，统一为短横线命名

### 2.3 虚拟维度补齐
Excel 数据偏重比例/姿态硬点参数。系统自动按车格/风格分组，用正态分布生成 6 个虚拟评分列（0-10 分），使五步法雷达图完整渲染：

| 维度 | 虚拟列 | 生成逻辑 |
|------|--------|----------|
| 型面 | 型面评分、曲面连续性评分 | 按造型风格分组 (运动7.5, 商务7.8, 硬派7.0, 稳重6.5) |
| 细节品质 | 品质评分、间隙段差评分 | 按车格分组 (大型8.0, 中型7.0, SUV7.5) |
| 工程平衡 | 工程评分、视野空间评分 | 按车格分组 (大型7.5, SUV8.0) |

### 2.4 性能优化
- `@st.cache_data(ttl=3600)` 缓存 Excel 数据，避免重复 I/O
- 侧边栏「🔄 刷新数据库」按钮一键清除缓存重新加载

---

## 三、五步法评价体系

### 3.1 维度-参数映射

| 维度 | 子参数 1 | 子参数 2 | 数据来源 |
|------|----------|----------|----------|
| 📐 比例 | 轮高车高比 (%) | 轴长比 (%) | 轮胎高度/车高, 轴距/车长 (真实计算) |
| 🚙 姿态 | 窗身比 (%) | 窗台线水平夹角 (°) | Excel 原始列 (真实数据) |
| ✨ 型面 | 型面评分 (0-10) | 曲面连续性评分 (0-10) | 虚拟生成 |
| 💎 细节品质 | 品质评分 (0-10) | 间隙段差评分 (0-10) | 虚拟生成 |
| ⚙️ 工程平衡 | 工程评分 (0-10) | 视野空间评分 (0-10) | 虚拟生成 |

### 3.2 评分算法

**核心函数**: `calculate_z_score_based_rating(value, mean, std, style, metric_name)`

```
Z-score = (value - mean) / std
基础分   = 5.0 + Z-score × 2.0    （裁剪至 [0, 10]）
风格加权 = ±0.3 ~ ±0.5            （根据风格偏好方向调整）
```

**风格偏好方向**:
| 风格 | 窗身比 | 窗台线夹角 | 轮高车高比 | 轴长比 |
|------|--------|-----------|-----------|--------|
| 运动 | ↓ 低分高 | ↓ 低分高 | ↑ 高分高 | — |
| 商务 | ↑ 高分高 | — | ↑ 高分高 | — |
| 硬派 | ↑ 高分高 | ↑ 高分高 | ↑ 高分高 | — |
| 稳重 | ↑ 高分高 | ↑ 高分高 | — | — |

维度最终得分 = 该维度下两个子参数得分的算术平均值。

---

## 四、可视化图表

### 4.1 紧凑五步法雷达图
- 位置: Vehicle Studio 第 6 列
- 尺寸: height=320（紧凑但信息完整）
- 红色填充: 在研车型 5 维度得分
- 蓝色虚线: 竞品基准线 (5.0)
- 范围: 0-10，带径向网格线
- 支持中文字体标注

### 4.2 设计空间坐标系 (Design Space Map) — 核心图表

**图层结构 (从底到顶)**:

| 图层 | 视觉表现 | 数据含义 |
|------|----------|----------|
| ① 灰色散点 | `#78909C` 圆点 size=8, opacity=0.55 | 过滤后的全部竞品车型 |
| ② 蓝色密度等高线 | `Histogram2dContour`, `#42A5F5` 半透明 | 竞品空间分布密度 |
| ③ 红色大五角星 ★ | `#EF5350`, size=28, star symbol, 白色描边 | **在研车型** (当前设计位置) |
| ④ 绿色菱形 ◆ | `#66BB6A`, size=16, diamond symbol | 销量>5000 爆款均值位置 |

**交互特性**:
- 5 个 Tab 各自独立 X/Y 轴参数选择器 (10+ 参数可选)
- 默认轴对齐对应维度: 比例Tab → X=轮高车高比,Y=轴长比
- 悬停工具提示: 显示车型名称 + 坐标精确值
- **实时联动**: 滑动侧边栏参数滑块 → 红色★ 在散点图中实时移动

---

## 五、交互功能清单

### 5.1 侧边栏控制面板
| 功能 | 说明 |
|------|------|
| 🔄 刷新数据库 | 清除缓存，重新加载 Excel |
| 项目代号 | 文本输入框 |
| 目标风格 | 下拉选择 (运动/商务/硬派/稳重) |
| 📷 图片上传 | 5 视角上传入口 (正侧/正前/正后/前45°/后45°)，支持 png/jpg/jpeg/webp |
| 车格过滤 | 下拉框，可选 "全部" 或特定车格 |
| 风格过滤 | 下拉框，可选 "全部" 或特定风格 |
| 竞品搜索多选 | 搜索框 + 多选列表，默认前 15 款 |
| 10 参数滑块 | 5 组 expander 折叠面板，默认值 = 竞品均值 |

### 5.2 设计空间分析 Tab
| 功能 | 说明 |
|------|------|
| X 轴参数选择 | 下拉框，10+ 参数可选 |
| Y 轴参数选择 | 下拉框，10+ 参数可选 |
| 散点密度图 | 竞品分布 + 密度等高线 + 红星定位 |
| 坐标信息 | 图表下方显示在研车型精确坐标 |

### 5.3 异常处理
- Excel 文件不存在 → 友好错误提示 + 路径说明
- 过滤结果为空 → 提示调整筛选条件
- 未选择竞品 → 提示至少选择一款
- 密度等高线绘制失败 → 静默跳过不中断

---

## 六、技术实现细节

### 6.1 文件清单
```
styling evaluation system/
├── app.py                       # 主程序 530+ 行
│   ├── Block 0: 常量定义 (DIMENSION_MAP, STYLE_PREFERENCES, PARAM_LABELS 等)
│   ├── Block 1: 数据 I/O (load_and_clean_data, 清洗/补齐/缓存)
│   ├── Block 2: 计算引擎 (Z-score 评分, 维度聚合)
│   ├── Block 3: 图表工厂 (compact_radar, design_space_map)
│   ├── Block 4: UI 渲染 (sidebar, vehicle_studio, design_space_tabs)
│   └── Block 5: 主流程 (数据→过滤→评分→渲染)
├── benchmark data.xlsx          # 136 款车型数据库
├── requirements.txt             # streamlit, pandas, numpy, plotly, scipy
└── .streamlit/config.toml       # 深色主题 (plotly_dark)
```

### 6.2 关键函数签名

```python
# 数据层
load_and_clean_data(file_path) → pd.DataFrame
_clean_percentage(val) → float
_gen_virtual_score(n, rng, df, group_col, style_means, default) → np.ndarray

# 计算引擎
calculate_z_score_based_rating(value, mean, std, style, metric_name) → float
calculate_dimension_scores(car_params, df_comp, style) → dict

# 图表工厂
create_compact_radar(dimension_scores, competitor_means) → go.Figure
create_design_space_map(df, x_param, y_param, car_x, car_y, hot_x, hot_y) → go.Figure

# UI 渲染
render_sidebar(df) → dict
render_vehicle_studio(dimension_scores, competitor_means, uploaded_images) → None
render_design_space_tabs(df, car_params) → None
```

### 6.3 运行方式
```bash
cd "E:/workspace-CA/styling evaluation system"
pip install -r requirements.txt
python -m streamlit run app.py
```
浏览器访问 `http://localhost:8501`

---

## 七、典型使用场景

**场景 1: 新车型概念评估**
1. 侧边栏输入项目代号 "Concept-X"，选择目标风格 "运动"
2. 上传 5 张概念草图/渲染图
3. 调整姿态参数滑块（如降低窗身比至 35%，降低窗台线夹角至 2.0°）
4. 观察雷达图得分变化，确认与运动风格的匹配度
5. 在比例 Tab 查看红色★是否落在主流竞品密度区

**场景 2: 竞品对标分析**
1. 在 «竞品搜索多选» 中只勾选 SUV 类型竞品
2. 车格过滤设为 "大型"，风格过滤设为 "商务"
3. 在研车型参数恢复默认值（竞品均值）
4. 切换不同 Tab，观察各维度下在研车型相对竞品空间位置

**场景 3: 数据刷新**
1. Excel 数据更新后，点击 🔄 按钮
2. 缓存清除，自动重新加载、清洗、补齐
3. 全局参数和图表实时更新

---

## 八、版本演进历史

| 版本 | 日期 | 核心变化 |
|------|------|----------|
| v1 MVP | 2026-07-22 | 10款模拟车型，雷达图+钟形曲线，基础评分 |
| v2 Design Studio | 2026-07-23 | 50聚类散点，Vehicle Visual Studio，Design Space Map (散点+密度+红星)，紧凑雷达图 |
| v3 Real Data | 2026-07-23 | 136款真实Excel数据，智能数据清洗，虚拟维度补齐，图片上传，全局过滤器，竞品搜索 |

---

*本文档由 Claude Code 生成 · 2026-07-23*
